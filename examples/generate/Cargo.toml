[package]
name = "generate"
version = "0.1.0"
edition = "2024"
authors = ["Neil Crago"]
description = "Example of generating a fractal using the tma_engine and fractal_algebra crates"

[dependencies]
fractal_algebra = "0.3.2"
tma_engine      = "0.1.3"
chrono = "0.4.41"
